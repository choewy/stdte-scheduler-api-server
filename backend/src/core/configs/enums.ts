export enum NodeEnvs {
  Local = 'local',
  Develop = 'develop',
  Staging = 'staging',
  Product = 'product',
}

export enum ConfigKey {
  Server = 'server',
  Typeorm = 'typeorm',
  Jwt = 'jwt',
  Kakao = 'kakao',
  Redis = 'redis',
}
