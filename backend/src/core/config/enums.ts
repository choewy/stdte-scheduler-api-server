export enum EnvKey {
  SERVER = 'SERVER',
  CORS = 'CORS',
  TYPEORM = 'TYPEORM',
  ACCESS_TOKEN = 'ACCESS_TOKEN',
  REFRESH_TOKEN = 'REFRESH_TOKEN',
  SWAGGER = 'SWAGGER',
}

export enum ConfigToken {
  Server = 'server',
  Cors = 'cors',
  Typeorm = 'typeorm',
  Auth = 'auth',
  Swagger = 'swagger',
}
